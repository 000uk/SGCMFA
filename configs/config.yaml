exp_name: stgcn_baseline
seed: 42

data:
  csv_path: data/jester/meta.csv
  data_dir: data/jester

model:
  in_channels: 3
  hidden_dim: 64
  num_layers: 3
  dropout: 0.5

train:
  epochs: 50
  batch_size: 32
  lr: 0.001
  weight_decay: 0.0001
  warmup_ratio: 0.1